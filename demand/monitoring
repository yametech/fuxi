gafana 所需要的需求：

1. 添加成员 (可做)

2. 添加team (可做)

3. 创建Folder (可做)

4. 团队和成员绑定，这里得根据创建用的时候返回的id去绑定 team (可做)

5. 创建视图模板给每个默认的Folder (可做)

6. 告警alter（调研ing）
 提供的维度:如果用户自定义的话,用户不会 需要我们自己写。可以先默认提供常用的维度，比如
 节点级别,workload,cpu,内存,磁盘,网络。
 
  规则： 
  1. 检测周期长度：例如持续1min
  2. 周期次数:  满足第一个规则的情况下,例如存在2次,就告警。
  3. 告警等级:  info， warn, error等。
  4. 危险告警 30 min 告警一次, 重要告警： 2h重复一次,一般告警不重复。当然可以让用户去设置

一个manager的配置文件复用


7. 单点登录 (调研ing)
先提供一个api的形式用，前端用 axios get 然后跳转
